import 'package:flutter/material.dart';

class StatPage1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      child: Text("First Statistics Page"),
    );
  }
}
